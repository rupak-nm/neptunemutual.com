import { css } from 'styled-components'

const regular = css`
  font-weight: 400;
`

const medium = css`
  font-weight: 500;
`

const semibold = css`
  font-weight: 600;
`

const bold = css`
  font-weight: 700;
`

export { bold, medium, regular, semibold }
